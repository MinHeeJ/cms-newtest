spring:
  datasource:
    url: jdbc:h2:mem:noticeboard;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
app:
  jwt-secret: TestSecret1234567890TestSecret1234567890
  admin-email: admin@example.com
  admin-password: Admin!2345
  admin-nickname: 관리자
